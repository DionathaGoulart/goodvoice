{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the window is allowed to ask the client for. `invoke` reaches this crate's own commands without a permission — they are the app, not a plugin — so the only thing that has to be granted is the event channel the roster, the call's health, mute and deafen all arrive on. Without it every `listen` is refused by the ACL and the window shows a room that never changes.",
  "windows": ["main"],
  "permissions": ["core:event:default"]
}
